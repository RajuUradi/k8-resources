apiVersion: v1
kind: Pod

metadata:
 name: nginx
 namespace: roboshop
 
spec:
 containers:
 - name: nginx
   image: nginx:latest
   resources:
   #soft limit
    requests:
     memory: "64mi" # RAM
     CPU: "100m" # CPU
   #hard limit
    limits:
     memory: "128mi" #RAM
     CPU: "150m" #CPU  